<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.koreate.cinema.mapper.MovieMapper">

    <select id="list" resultType="net.koreate.cinema.vo.MovieVO">
        SELECT * FROM final_movie ORDER BY movie_code DESC
    </select>

    <insert id="insert">
        INSERT INTO final_movie
        (title, director, genre, release_date, age_limit, story, running_time)
        VALUES
        (#{title}, #{director}, #{genre}, #{release_date}, #{age_limit}, #{story}, #{running_time})
    </insert>

    <select id="read" resultType="net.koreate.cinema.vo.MovieVO">
        SELECT * FROM final_movie WHERE movie_code = #{movie_code}
    </select>

    <delete id="delete">
        DELETE FROM final_movie WHERE movie_code = #{movie_code}
    </delete>

</mapper>