<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">
	<insert id="insertMember" parameterType="net.koreate.cinema.vo.MemberVO">
		INSERT INTO final_member (id, pass, name, nickname, phone, email, gender, age)
		VALUES(#{id},#{pass},#{name},#{nickname},#{phone},#{email},#{gender},#{age})
	</insert>
	
	<select id="readMemberWithPass" parameterType="map" resultType="net.koreate.cinema.vo.MemberVO">
		SELECT * FROM final_member WHERE id = #{id} AND pass = #{pass}
	</select>
	
	<select id="idCheck" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM final_member WHERE id = #{id}
	</select>
	
	<update id="updateMember" parameterType="net.koreate.cinema.vo.MemberVO">
		UPDATE final_member 
		SET pass=#{pass}, name=#{name}, nickname=#{nickname}, phone=#{phone}, email=#{email}, gender=#{gender}, age=#{age}
		WHERE id=#{id}
	</update>
	
	<delete id="removeMember" parameterType="int">
		DELETE FROM final_member WHERE num = #{num}
	</delete>
	
	<select id="readMemberList" resultType="net.koreate.cinema.vo.MemberVO">
		SELECT * FROM final_member WHERE id != 'admin' ORDER BY num DESC
	</select>
</mapper>